version: '3.8'

services:
  api:
    command: [ "python", "manage.py", "runserver", "0.0.0.0:8000" ]
    environment:
      - DEBUG=true
    volumes:
      - ./backend:/app
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend:/app
